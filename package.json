{
  "name": "kahn-quiz-local-react-vite",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first React Vite quiz creator and player with localStorage, offline cache, and local JSON import/export.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "katex": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
